# Web framework and ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Request/response schema validation (FastAPI depends on this)
pydantic==2.7.1

# HTTP client — used by the HEALTHCHECK curl alternative and future integrations
httpx==0.27.0
